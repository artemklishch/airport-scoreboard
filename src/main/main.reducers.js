const initialContentState = {
  contentForFourBlocksOfContent: [],
};
export const contentReducer = (state = initialContentState, action) => {
  switch(action.type){
    default: return state;
  };
};